<h1><%= t('views.home.index.title') %></h1>

<% @jobs.each do |job| %>
  <div>
    <h3><%= job.title %></h3>
    <p><%= job.description %></p>
    <%= link_to t('views.home.index.see_more'), job_path(job.id) %>
  </div>
  <hr />
<% end %>
